let initial = {
    friends: [ 
        {id: 1, name: 'Andrey'},
        {id: 2, name: 'Yulia'},
        {id: 3, name: 'Jenia'},
        {id: 4, name: 'Aleks'}
    ]
}

const navbarReducer = (state = initial, action) => {
    return state;
}

export default navbarReducer;